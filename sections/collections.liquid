{%- if section.blocks.size > 0 -%}
  {{ "collections.css" | asset_url | stylesheet_tag }}

  {%- assign color_scheme           = section.settings.color_scheme -%}
  {%- assign button_label           = section.settings.button_label -%}
  {%- assign button_url             = section.settings.button_url -%}
  {%- assign grid                   = section.settings.grid -%}
  {%- assign image_placeholder      = settings.image_placeholder -%}
  {%- assign overlay_background     = section.settings.overlay_background -%}
  {%- assign overlay_color          = section.settings.overlay_color -%}
  {%- assign overlay_opacity        = section.settings.overlay_opacity -%}
  {%- assign padding_top            = section.settings.padding_top -%}
  {%- assign padding_bottom         = section.settings.padding_bottom -%}
  {%- assign padding_top_mobile     = section.settings.padding_top_mobile -%}
  {%- assign padding_bottom_mobile  = section.settings.padding_bottom_mobile -%}
  {%- assign show_borders           = section.settings.show_borders -%}
  {%- assign show_overlay           = section.settings.show_overlay -%}
  {%- assign spacing                = section.settings.spacing -%}
  {%- assign spacing_mobile         = section.settings.spacing_mobile -%}
  {%- assign title                  = section.settings.title -%}

  {% comment %} CSS variables start {% endcomment %}
  {%- capture variables -%}
    {%- if show_overlay -%}
      --color-overlay: {{- overlay_color -}};
      --background-overlay: {{- overlay_background -}};
      --background-overlay-00: {{- overlay_background | append: "00" -}};
      --opacity-overlay: {{ overlay_opacity | append: "%"}};
    {%- endif -%}

    {%- case padding_top -%}
      {%- when 'small' -%}
        --padding-top: 24px;
      {%- when 'medium' -%}
        --padding-top: 68px;
      {%- when 'large' -%}
        --padding-top: 112px;
    {%- endcase -%}

    {%- case padding_bottom -%}
      {%- when 'small' -%}
        --padding-bottom: 24px;
      {%- when 'medium' -%}
        --padding-bottom: 68px;
      {%- when 'large' -%}
        --padding-bottom: 112px;
    {%- endcase -%}

    {%- case padding_top_mobile -%}
      {%- when 'small' -%}
        --padding-top-mobile: 18px;
      {%- when 'medium' -%}
        --padding-top-mobile: 40px;
      {%- when 'large' -%}
        --padding-top-mobile: 60px;
    {%- endcase -%}

    {%- case padding_bottom_mobile -%}
      {%- when 'small' -%}
        --padding-bottom-mobile: 18px;
      {%- when 'medium' -%}
        --padding-bottom-mobile: 40px;
      {%- when 'large' -%}
        --padding-bottom-mobile: 60px;
    {%- endcase -%}

    {%- case grid -%}
      {%- when 'two' -%}
        --template-columns: 2;
      {%- when 'three' -%}
        --template-columns: 3;
      {%- when 'four' -%}
        --template-columns: 4;
      {%- when 'five' -%}
        --template-columns: 5;
    {%- endcase -%}

    {%- case spacing -%}
      {%- when 'none' -%}
        --grid-gap: 0;
      {%- when 'small' -%}
        --grid-gap: 4px;
      {%- when 'medium' -%}
        --grid-gap: 16px;
      {%- when 'large' -%}
        --grid-gap: 32px;
    {%- endcase -%}

    {%- case spacing_mobile -%}
      {%- when 'none' -%}
        --grid-gap-mobile: 0;
      {%- when 'small' -%}
        --grid-gap-mobile: 4px;
      {%- when 'medium' -%}
        --grid-gap-mobile: 16px;
      {%- when 'large' -%}
        --grid-gap-mobile: 32px;
    {%- endcase -%}
  {%- endcapture -%}
  {% comment %} CSS variables end {% endcomment %}

  <div
    class="collections__wrapper collections__wrapper-{{ grid }} color-{{ color_scheme.id }}{% if show_borders %} collection__wrapper--with-border{% endif %}{% if show_overlay %} collection__wrapper--with-overlay{% endif %}{% if padding_top != blank or padding_top_mobile != blank %} collections__wrapper--padding-top{% endif %}{% if padding_bottom != blank or padding_bottom_mobile != blank %} collections__wrapper--padding-bottom{%- endif -%}"
    style="{{- variables | escape -}}"
  >
      <div class="collections__container container">
        {%- if title != blank or button_label != blank and button_url != blank -%}
          <div class="collections__top">
            {%- if title != blank -%}
              <h2 class="collections__title">{{ title }}</h2>
            {%- endif -%}

            {%- if button_label != blank and button_url != blank -%}
              <a href="{{ button_url }}" class="collections__button button button--outlined">
                {{- button_label -}}
              </a>
            {%- endif -%}
          </div>
        {%- endif -%}

        {%- render 'collections-grid',
            items: section.blocks,
            items_type: "blocks",
            placeholder: image_placeholder,
        -%}
      </div>
  </div>
{%- endif -%}

{% schema %}
  {
    "name": "Collections",
    "description": "Add a list of collections to your page",
    "tag": "section",
    "class": "collections",
    "settings": [
      {
        "type": "header",
        "content": "General settings"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "Collections",
        "info": "Leave blank to hide the title"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Button label",
        "default": "SEE ALL",
        "info": "Leave blank to hide the button"
      },
      {
        "type": "url",
        "id": "button_url",
        "label": "Button url",
        "default": "booqable://collections"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "set-4"
      },
      {
        "type": "checkbox",
        "id": "show_borders",
        "label": "Show borders",
        "default": true
      },
      {
        "type": "header",
        "content": "Overlay"
      },
      {
        "type": "checkbox",
        "id": "show_overlay",
        "label": "Show overlay",
        "default": true
      },
      {
        "type": "color",
        "id": "overlay_color",
        "label": "Color",
        "default": "#FFFFFF"
      },
      {
        "type": "color",
        "id": "overlay_background",
        "label": "Background color",
        "default": "#000000"
      },
      {
        "type": "number",
        "id": "overlay_opacity",
        "label": "Background percentage",
        "min": 0,
        "max": 100,
        "default": 100
      },
      {
        "type": "header",
        "content": "Desktop settings"
      },
      {
        "type": "select",
        "id": "grid",
        "label": "Layout",
        "options": [
          {
            "value": "two",
            "label": "2 columns"
          },
          {
            "value": "three",
            "label": "3 columns"
          },
          {
            "value": "four",
            "label": "4 columns"
          },
          {
            "value": "five",
            "label": "5 columns"
          }
        ],
        "default": "three"
      },
      {
        "type": "select",
        "id": "spacing",
        "label": "Spacing between columns",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      },
      {
        "type": "select",
        "id": "padding_top",
        "label": "Padding top",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      },
      {
        "type": "select",
        "id": "padding_bottom",
        "label": "Padding bottom",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      },
      {
        "type": "header",
        "content": "Mobile settings"
      },
      {
        "type": "select",
        "id": "spacing_mobile",
        "label": "Spacing between columns",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      },
      {
        "type": "select",
        "id": "padding_top_mobile",
        "label": "Padding top",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      },
      {
        "type": "select",
        "id": "padding_bottom_mobile",
        "label": "Padding bottom",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "none"
      }
    ],
    "max_blocks": 10,
    "blocks": [
      {
        "type": "collection",
        "name": "Collection",
        "settings": [
          {
            "type": "collection",
            "id": "collection",
            "label": "Collection"
          },
          {
            "type": "text",
            "id": "collection_button_label",
            "label": "Button label",
            "default": "SHOP NOW",
            "info": "Leave blank to hide the button"
          },
          {
            "type": "header",
            "content": "Badge settings"
          },
          {
            "type": "text",
            "id": "badge_label",
            "label": "Label",
            "default": "NEW!",
            "info": "Leave blank to hide the budge"
          },
          {
            "type": "color",
            "id": "badge_color",
            "label": "Text color",
            "default": "#FFFFFF"
          },
          {
            "type": "color",
            "id": "badge_background",
            "label": "Background color",
            "default": "#FF0000"
          }
        ]
      }
    ]
  }
{% endschema %}
