{{ "hero.css" | asset_url | stylesheet_tag }}

{%- assign button_primary_label    = section.settings.button_primary_label -%}
{%- assign button_primary_url      = section.settings.button_primary_url -%}
{%- assign button_outline_label    = section.settings.button_outline_label -%}
{%- assign button_outline_url      = section.settings.button_outline_url -%}
{%- assign color_scheme            = section.settings.color_scheme -%}
{%- assign description             = section.settings.description -%}
{%- assign full_width              = section.settings.full_width -%}
{%- assign full_width_content      = section.settings.full_width_content -%}
{%- assign height                  = section.settings.height -%}
{%- assign height_mobile           = section.settings.height_mobile -%}
{%- assign image                   = section.settings.image -%}
{%- assign image_mobile            = section.settings.image_mobile -%}
{%- assign overlay_background      = section.settings.overlay_background -%}
{%- assign overlay_color           = section.settings.overlay_color -%}
{%- assign overlay_opacity         = section.settings.overlay_opacity -%}
{%- assign padding_bottom          = section.settings.padding_bottom -%}
{%- assign padding_bottom_mobile   = section.settings.padding_bottom_mobile -%}
{%- assign padding_top             = section.settings.padding_top -%}
{%- assign padding_top_mobile      = section.settings.padding_top_mobile -%}
{%- assign show_datepicker         = section.settings.show_datepicker -%}
{%- assign show_overlay            = section.settings.show_overlay -%}
{%- assign text_position           = section.settings.text_position -%}
{%- assign text_position_mobile    = section.settings.text_position_mobile -%}
{%- assign title                   = section.settings.title -%}
{%- assign video_url               = section.settings.video_url | strip -%}

{% comment %} CSS variables start {% endcomment %}
{%- capture variables -%}
  {%- case height -%}
    {%- when 'small' -%}
      --height: 700px;
    {%- when 'medium' -%}
      --height: 853px;
    {%- when 'full' -%}
      --height: 100vh;
  {%- endcase -%}

  {%- case height_mobile -%}
    {%- when 'auto' -%}
      --height-mobile: auto;
    {%- when 'medium' -%}
      --height-mobile: 550px;
    {%- when 'full' -%}
      --height-mobile: calc(100vh - var(--header-height, 109px));
  {%- endcase -%}

  {%- case padding_top -%}
    {%- when 'small' -%}
      --padding-top: 30px;
    {%- when 'medium' -%}
      --padding-top: 60px;
    {%- when 'large' -%}
      --padding-top: 100px;
  {%- endcase -%}

  {%- case padding_bottom -%}
    {%- when 'small' -%}
      --padding-bottom: 30px;
    {%- when 'medium' -%}
      --padding-bottom: 60px;
    {%- when 'large' -%}
      --padding-bottom: 100px;
  {%- endcase -%}

  {%- case padding_top_mobile -%}
    {%- when 'small' -%}
      --padding-top-mobile: 16px;
    {%- when 'medium' -%}
      --padding-top-mobile: 32px;
    {%- when 'large' -%}
      --padding-top-mobile: 64px;
  {%- endcase -%}

  {%- case padding_bottom_mobile -%}
    {%- when 'small' -%}
      --padding-bottom-mobile: 16px;
    {%- when 'medium' -%}
      --padding-bottom-mobile: 32px;
    {%- when 'large' -%}
      --padding-bottom-mobile: 64px;
  {%- endcase -%}

  {%- case text_position -%}
    {%- when 'top-left' -%}
      --date-picker-alignment: flex-start;
      --text-justify-content: flex-start;
      --text-align-items: flex-start;
      --text-align: left;
      --margin-right: auto;
      --margin-left: 0;
    {%- when 'top-center' -%}
      --date-picker-alignment: center;
      --text-justify-content: center;
      --text-align-items: flex-start;
      --text-align: center;
      --margin-right: auto;
      --margin-left: auto;
    {%- when 'top-right' -%}
      --date-picker-alignment: flex-end;
      --text-justify-content: flex-end;
      --text-align-items: flex-start;
      --text-align: right;
      --margin-right: 0;
      --margin-left: auto;
    {%- when 'middle-left' -%}
      --date-picker-alignment: flex-start;
      --text-justify-content: flex-start;
      --text-align-items: center;
      --text-align: left;
      --margin-right: auto;
      --margin-left: 0;
    {%- when 'center' -%}
      --date-picker-alignment: center;
      --text-justify-content: center;
      --text-align-items: center;
      --text-align: center;
      --margin-right: auto;
      --margin-left: auto;
    {%- when 'middle-right' -%}
      --date-picker-alignment: flex-end;
      --text-justify-content: flex-end;
      --text-align-items: center;
      --text-align: right;
      --margin-right: 0;
      --margin-left: auto;
    {%- when 'bottom-left' -%}
      --date-picker-alignment: flex-start;
      --text-justify-content: flex-start;
      --text-align-items: flex-end;
      --text-align: left;
      --margin-right: auto;
      --margin-left: 0;
    {%- when 'bottom-center' -%}
      --date-picker-alignment: center;
      --text-justify-content: center;
      --text-align-items: flex-end;
      --text-align: center;
      --margin-right: auto;
      --margin-left: auto;
    {%- when 'bottom-right' -%}
      --date-picker-alignment: flex-end;
      --text-justify-content: flex-end;
      --text-align-items: flex-end;
      --text-align: right;
      --margin-right: 0;
      --margin-left: auto;
  {%- endcase -%}

  {%- case text_position_mobile -%}
    {%- when 'top-left' -%}
      --date-picker-alignment-mobile: flex-start;
      --text-justify-content-mobile: flex-start;
      --text-align-items-mobile: flex-start;
      --text-align-mobile: left;
      --margin-right-mobile: auto;
      --margin-left-mobile: 0;
    {%- when 'top-center' -%}
      --date-picker-alignment-mobile: center;
      --text-justify-content-mobile: center;
      --text-align-items-mobile: flex-start;
      --text-align-mobile: center;
      --margin-right-mobile: auto;
      --margin-left-mobile: auto;
    {%- when 'top-right' -%}
      --date-picker-alignment-mobile: flex-end;
      --text-justify-content-mobile: flex-end;
      --text-align-items-mobile: flex-start;
      --text-align-mobile: right;
      --margin-right-mobile: 0;
      --margin-left-mobile: auto;
    {%- when 'middle-left' -%}
      --date-picker-alignment-mobile: flex-start;
      --text-justify-content-mobile: flex-start;
      --text-align-items-mobile: center;
      --text-align-mobile: left;
      --margin-right-mobile: auto;
      --margin-left-mobile: 0;
    {%- when 'center' -%}
      --date-picker-alignment-mobile: center;
      --text-justify-content-mobile: center;
      --text-align-items-mobile: center;
      --text-align-mobile: center;
      --margin-right-mobile: auto;
      --margin-left-mobile: auto;
    {%- when 'middle-right' -%}
      --date-picker-alignment-mobile: flex-end;
      --text-justify-content-mobile: flex-end;
      --text-align-items-mobile: center;
      --text-align-mobile: right;
      --margin-right-mobile: 0;
      --margin-left-mobile: auto;
    {%- when 'bottom-left' -%}
      --date-picker-alignment-mobile: flex-start;
      --text-justify-content-mobile: flex-start;
      --text-align-items-mobile: flex-end;
      --text-align-mobile: left;
      --margin-right-mobile: auto;
      --margin-left-mobile: 0;
    {%- when 'bottom-center' -%}
      --date-picker-alignment-mobile: center;
      --text-justify-content-mobile: center;
      --text-align-items-mobile: flex-end;
      --text-align-mobile: center;
      --margin-right-mobile: auto;
      --margin-left-mobile: auto;
    {%- when 'bottom-right' -%}
      --date-picker-alignment-mobile: flex-end;
      --text-justify-content-mobile: flex-end;
      --text-align-items-mobile: flex-end;
      --text-align-mobile: right;
      --margin-right-mobile: 0;
      --margin-left-mobile: auto;
  {%- endcase -%}

  {%- if show_overlay -%}
    --color-overlay: {{- overlay_color -}};
    --background-overlay: {{- overlay_background -}};
    --background-overlay-00: {{- overlay_background | append: "00" -}};
    --opacity-overlay: {{ overlay_opacity | append: "%"}};

    {%- case text_position -%}
      {%- when 'top-left' -%}
        --overlay-direction: 127deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'top-center' -%}
        --overlay-direction: 180deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'top-right' -%}
        --overlay-direction: 217deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'middle-left' -%}
        --overlay-direction: 127deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'center' -%}
        --overlay-direction: 180deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'middle-right' -%}
        --overlay-direction: 217deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'bottom-left' -%}
        --overlay-direction: 37deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'bottom-center' -%}
        --overlay-direction: 0deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
      {%- when 'bottom-right' -%}
        --overlay-direction: 307deg;
        --linear-gradient: linear-gradient(var(--overlay-direction), var(--background-overlay) 0%, var(--background-overlay) 32.73%, var(--background-overlay-00) 100%);
    {%- endcase -%}

    {%- case text_position_mobile -%}
      {%- when 'top-left' -%}
        --overlay-direction-mobile: 127deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'top-center' -%}
        --overlay-direction-mobile: 180deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'top-right' -%}
        --overlay-direction-mobile: 217deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'middle-left' -%}
        --overlay-direction-mobile: 127deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'center' -%}
        --overlay-direction-mobile: 180deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'middle-right' -%}
        --overlay-direction-mobile: 217deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'bottom-left' -%}
        --overlay-direction-mobile: 37deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'bottom-center' -%}
        --overlay-direction-mobile: 0deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
      {%- when 'bottom-right' -%}
        --overlay-direction-mobile: 307deg;
        --linear-gradient-mobile: linear-gradient(var(--overlay-direction-mobile), var(--background-overlay) 0%, var(--background-overlay) 41.53%, var(--background-overlay-00) 100%);
    {%- endcase -%}
  {%- endif -%}
{%- endcapture -%}
{% comment %} CSS variables end {% endcomment %}

{%- unless full_width -%}
  <div class="hero__container container">
{%- endunless -%}
    <div class="hero__wrapper{% if padding_top != blank or padding_top_mobile != blank %} hero__wrapper--padding-top{% endif %}{% if padding_bottom != blank or padding_bottom_mobile != blank %} hero__wrapper--padding-bottom{% endif %} color-{{- color_scheme.id -}}" style="{{- variables | escape -}}">
      {%- if full_width -%}
        <div class="hero__container container">
      {%- endif -%}
          <div class="hero__text-area{% if full_width_content %} hero__text-area--max-width{% endif %}{% if show_overlay %} hero__text-area--color{%- endif -%}">
            <div class="hero__content">
              {%- if title != blank -%}
                <h1 class="hero__title">{{- title -}}</h1>
              {%- endif -%}

              {%- if description != blank -%}
                <div class="hero__description text-medium bq-content rx-content">{{- description -}}</div>
              {%- endif -%}

              {%- if show_datepicker -%}
                <div class="hero__date-picker-wrapper">
                  {%- render 'date-picker', key: section.key -%}
                </div>
              {%- endif -%}

              {%- if button_primary_label != blank or button_outline_label != blank -%}
                <div class="hero__buttons">
                  {%- if button_primary_label != blank and button_primary_url != blank -%}
                    <a href="{{ button_primary_url }}" class="hero__button button button--primary button--large">{{- button_primary_label -}}</a>
                  {%- endif -%}

                  {%- if button_outline_label != blank and button_outline_url != blank -%}
                    <a href="{{ button_outline_url }}" class="hero__button button button--outlined button--large">{{- button_outline_label -}}</a>
                  {%- endif -%}
                </div>
              {%- endif -%}
            </div>
          </div>
      {%- if full_width -%}
        </div>
      {%- endif -%}

      <div class="hero__vision{% if video_url != blank %} hero__vision--video{%- endif -%}">
        {%- if video_url contains "youtube.com" -%}
          {%- assign video_drop = video_url | split: "?v=" -%}
          {%- assign video_id   = video_drop | last | strip -%}

          <div class="hero__vision-wrapper{% if show_overlay %} hero__vision-wrapper--overlay{%- endif -%}">
            <iframe
              class="hero__video"
              width="1920"
              height="1080"
              src="https://www.youtube.com/embed/{{- video_id -}}?playlist={{- video_id -}}&autoplay=1&mute=1&loop=1&controls=0&disablekb=1&fs=0&playsinline=1&enablejsapi=1&iv_load_policy=3&modestbranding=1"
              frameborder="0"
              allow="autoplay; muted; encrypted-media; fullscreen"
              allowfullscreen
              autoplay
              loop
              mute
              playsinline
            ></iframe>
          </div>

        {%- elsif video_url contains "vimeo.com" -%}
          {%- assign video_drop = video_url | split: "/" -%}
          {%- assign video_id   = video_drop | last | strip -%}

          <div class="hero__vision-wrapper{% if show_overlay %} hero__vision-wrapper--overlay{%- endif -%}">
            <iframe
              class="hero__video"
              src="https://player.vimeo.com/video/{{- video_id -}}?autoplay=1&muted=1&loop=1&autopause=0&controls=0&background=1"
              width="1920"
              height="1080"
              frameborder="0"
              allow="autoplay; fullscreen"
              allowfullscreen
              autoplay
              muted
              loop
            ></iframe>
          </div>

        {%- elsif image.url != blank or image_mobile.url != blank -%}
          {%- if image.url != blank -%}
            <div class="hero__vision-wrapper{% if image_mobile != blank %} hero__vision-wrapper--desktop{% endif %}{% if show_overlay %} hero__vision-wrapper--overlay{%- endif -%}">
              {%- render 'image',
                image: image,
                loading: 'lazy',
                class: 'hero__image',
                size: 'xxl'
              -%}
            </div>
          {%- endif -%}

          {%- if image_mobile.url != blank -%}
            <div class="hero__vision-wrapper hero__vision-wrapper--mobile{% if show_overlay %} hero__vision-wrapper--overlay{%- endif -%}">
              {%- render 'image',
                image: image_mobile,
                loading: 'lazy',
                class: 'hero__image',
                size: 'xxl'
              -%}
            </div>
          {%- endif -%}
        {%- endif -%}
      </div>
    </div>
{% unless full_width %}
  </div>
{%- endunless -%}

{% schema %}
  {
    "name": "Hero",
    "tag": "section",
    "class": "hero",
    "description": "Engage visitors by giving great first impression",
    "settings": [
      {
        "type": "header",
        "content": "General settings"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "set-4"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "Everything for <br>your day!"
      },
      {
        "type": "contentEditor",
        "id": "description",
        "label": "Description",
        "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. <br>Suspendisse varius enim in eros elementum tristique."
      },
      {
        "type": "checkbox",
        "id": "full_width_content",
        "label": "Full-width text content",
        "default": false
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Image",
        "default": "booqable://assets/image-hero.jpg"
      },
      {
        "type": "image_picker",
        "id": "image_mobile",
        "label": "Image mobile (optional)"
      },
      {
        "type": "text",
        "id": "video_url",
        "label": "Video URL (optional)",
        "info": "Youtube, Vimeo. If you paste video link here, any images will be ignored. Preferred format 16:9"
      },
      {
        "type": "checkbox",
        "id": "full_width",
        "label": "Full-width background",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_datepicker",
        "default": true,
        "label": "Show period picker"
      },
      {
        "type": "header",
        "content": "Overlay"
      },
      {
        "type": "checkbox",
        "id": "show_overlay",
        "label": "Show overlay",
        "default": true
      },
      {
        "type": "color",
        "id": "overlay_background",
        "label": "Overlay background color",
        "default": "#F5FAFA"
      },
      {
        "type": "color",
        "id": "overlay_color",
        "label": "Overlay text color",
        "default": "#2F4044"
      },
      {
        "type": "number",
        "id": "overlay_opacity",
        "label": "Overlay percentage",
        "min": 0,
        "max": 100,
        "default": 72
      },
      {
        "type": "header",
        "content": "Buttons"
      },
      {
        "type": "text",
        "id": "button_primary_label",
        "label": "Button 1 label",
        "default": "Check out products"
      },
      {
        "type": "url",
        "id": "button_primary_url",
        "label": "Button 1 link",
        "default": "booqable://products"
      },
      {
        "type": "text",
        "id": "button_outline_label",
        "label": "Button 2 label",
        "default": "More info"
      },
      {
        "type": "url",
        "id": "button_outline_url",
        "label": "Button 2 link",
        "default": "booqable://products"
      },
      {
        "type": "header",
        "content": "Desktop settings"
      },
      {
        "type": "select",
        "id": "height",
        "label": "Section height",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "full",
            "label": "Full"
          }
        ],
        "default": "medium"
      },
      {
        "type": "select",
        "id": "text_position",
        "label": "Text position",
        "options": [
          {
            "value": "top-left",
            "label": "Top/Left"
          },
          {
            "value": "top-center",
            "label": "Top/Center"
          },
          {
            "value": "top-right",
            "label": "Top/Right"
          },
          {
            "value": "middle-left",
            "label": "Middle/Left"
          },
          {
            "value": "center",
            "label": "Center"
          },
          {
            "value": "middle-right",
            "label": "Middle/Right"
          },
          {
            "value": "bottom-left",
            "label": "Bottom/Left"
          },
          {
            "value": "bottom-center",
            "label": "Bottom/Center"
          },
          {
            "value": "bottom-right",
            "label": "Bottom/Right"
          }
        ],
        "default": "middle-left"
      },
      {
        "type": "select",
        "id": "padding_top",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "medium"
      },
      {
        "type": "select",
        "id": "padding_bottom",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "medium"
      },
      {
        "type": "header",
        "content": "Mobile settings"
      },
      {
        "type": "select",
        "id": "height_mobile",
        "label": "Section height",
        "options": [
          {
            "value": "auto",
            "label": "Auto"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "full",
            "label": "Full"
          }
        ],
        "default": "full"
      },
      {
        "type": "select",
        "id": "text_position_mobile",
        "label": "Text position",
        "options": [
          {
            "value": "top-left",
            "label": "Top/Left"
          },
          {
            "value": "top-center",
            "label": "Top/Center"
          },
          {
            "value": "top-right",
            "label": "Top/Right"
          },
          {
            "value": "middle-left",
            "label": "Middle/Left"
          },
          {
            "value": "center",
            "label": "Center"
          },
          {
            "value": "middle-right",
            "label": "Middle/Right"
          },
          {
            "value": "bottom-left",
            "label": "Bottom/Left"
          },
          {
            "value": "bottom-center",
            "label": "Bottom/Center"
          },
          {
            "value": "bottom-right",
            "label": "Bottom/Right"
          }
        ],
        "default": "bottom-center"
      },
      {
        "type": "select",
        "id": "padding_top_mobile",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "select",
        "id": "padding_bottom_mobile",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "small"
      }
    ]
  }
{% endschema %}
